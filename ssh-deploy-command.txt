# SSH DEPLOYMENT COMMAND - UPDATED
# Bu komutu SSH panelinde çalıştırın:

chmod +x deploy-nexaven.sh && ./deploy-nexaven.sh

# Alternatif olarak tek satırda:
chmod +x deploy-nexaven.sh; ./deploy-nexaven.sh

# Deployment sonrası kontrol komutları (nexaven-website dizininde):
cd nexaven-website
docker compose ps                    # Container durumları
docker compose logs -f              # Canlı loglar
curl http://localhost/health         # Health check
curl http://localhost/ollama/api/tags # Ollama proxy test
curl http://localhost/car_ecu_data/Bmw/Bmw_20260122_160652.json  # ECU data test

# Sorun olursa:
docker compose down && docker compose up -d --build

# Nginx config test:
docker compose exec nginx nginx -t

# Container logları:
docker compose logs nginx
docker compose logs frontend